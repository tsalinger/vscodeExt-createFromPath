{
    "name": "foldercreator",
    "displayName": "FolderCreator",
    "description": "Create multiple folders at once",
    "version": "0.0.1",
    "publisher": "publisher",
    "engines": {
        "vscode": "^1.19.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:extension.createNewFolders"
    ],
    "main": "./out/extension",
    "contributes": {
        "menus": {
            "commandPalette": [
                {
                    "command": "extension.createNewFolders"
                }
            ],
            "explorer/context": [
                {
                    "command": "extension.createNewFolders",
                    "group": "navigation@3"
                }
            ]
        },
        "commands": [
            {
                "command": "extension.createNewFolders",
                "title": "New Folders From Path"
            }
        ]
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "npm run compile && node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "typescript": "^2.6.1",
        "vscode": "^1.1.6",
        "@types/node": "^7.0.43",
        "@types/mocha": "^2.2.42"
    }
}
